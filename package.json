{
  "name": "squaredlabs",
  "version": "1.0.0",
  "description": "The official team website (in development)",
  "author": "squredlabs",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt generate",
    "clean": "rm logs/*; rm -rf dist/;rm -rf ./.nuxt"
  },
  "devDependencies": {
    "node-sass": "^4.9.0",
    "sass-loader": "^7.0.1"
  },
  "dependencies": {
    "nuxt": "^1.4.0",
    "vue-resource": "^1.5.0"
  }
}
